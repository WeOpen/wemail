# 🦴 shared/skeleton

共享骨架屏原语层。

## ✅ 放什么
- `Skeleton`
- `rect / text / circle` 形状
- 自定义尺寸、圆角和可选 shimmer 动画
- 默认装饰语义，必要时可宣布“内容加载中”

## 🚫 不放什么
- 页面级骨架布局编排
- 实际数据加载策略
- 业务专属占位内容文案

## 使用约定
- 默认静态，占位块只负责稳定布局
- 需要动效时显式传 `animated`
- 需要读屏器播报时传 `announce`
