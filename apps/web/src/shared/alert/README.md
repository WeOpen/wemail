# 🚨 shared/alert

共享静态提示原语层。

## ✅ 放什么
- `Alert`
- 信息 / 成功 / 警告 / 错误提示
- 标题、正文、可选操作区与关闭按钮

## 🚫 不放什么
- Toast 级自动消失逻辑
- 业务错误映射与重试状态机
- 页面级布局编排

## 可访问性
- 默认 `role="alert"`
- 关闭按钮必须带 `dismissLabel`
- 图标默认 `aria-hidden`
