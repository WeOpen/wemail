# 🎨 shared/styles

全局样式与共享视觉资产。

## ✅ 放什么
- 全局样式入口
- 主题变量
- 通用布局 / 排版基线
- `tokens.css` 里的 design token
- `base.css` 里的 reset / 根布局 / 裸元素基线
- `primitives.css` 里的原语样式导入汇总

## 🚫 不放什么
- 页面专属大段内联样式说明
- 与单个业务强绑定的视觉逻辑
